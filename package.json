{
  "name": "backend_project",
  "version": "1.0.0",
  "description": "",
  "repository": {
    "type": "git",
    "url": "no"
  },
  "license": "MIT",
  "author": "lex",
  "type": "commonjs",
  "main": "index.js",
  "scripts": {
    "start": "cd backend && node index.js",
    "build": "cd backend && npm install && cd ../fronted && npm install && npm run build && cd .. && rm -rf backend/dist && cp -r fronted/dist backend/",
    "install": "cd backend && npm install && cd ../fronted && npm install",
    "dev": "node --watch ./backend/index.js",
    "frontend": "cd fronted && npm run dev",
    "backend": "cd backend && npm run dev",
    "dev:both": "concurrently \"npm run backend\" \"npm run frontend\"",
    "install:all": "npm install && cd backend && npm install && cd ../fronted && npm install",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "concurrently": "^9.2.0"
  },
  "dependencies": {
    "axios": "^1.10.0",
    "bcrypt": "^6.0.0",
    "cors": "^2.8.5",
    "dotenv": "^17.2.0",
    "express": "^4.16.2",
    "express-async-errors": "^3.1.1",
    "fs": "^0.0.1-security",
    "jsonwebtoken": "^9.0.2",
    "mongoose": "^8.16.3",
    "path": "^0.12.7",
    "readline": "^1.3.0",
    "react-icons": "^5.5.0",
    "react-router-dom": "^7.6.3"
  }
}
