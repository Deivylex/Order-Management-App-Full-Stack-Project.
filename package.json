{
  "name": "backend_project",
  "version": "1.0.0",
  "description": "",
  "repository": {
    "type": "git",
    "url": "no"
  },
  "license": "MIT",
  "author": "lex",
  "type": "commonjs",
  "main": "index.js",
  "scripts": {
    "start": " node index.js",
    "dev": "node --watch ./backend/index.js",
    "frontend": "cd fronted && npm run dev",
    "backend": "cd backend && npm run dev",
    "dev:both": "concurrently \"npm run backend\" \"npm run frontend\"",
    "install:all": "npm install && cd backend && npm install && cd ../fronted && npm install",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "concurrently": "^9.2.0"
  },
  "dependencies": {
    "react-icons": "^5.5.0",
    "react-router-dom": "^7.6.3"
  }
}
